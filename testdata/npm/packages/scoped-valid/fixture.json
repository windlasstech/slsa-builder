{
  "phase": "build-pack",
  "package-directory": "testdata/npm/packages/scoped-valid",
  "package-manager": {
    "name": "pnpm",
    "version": "10.14.0",
    "selection-source": "packageManager",
    "selected-lockfile": "pnpm-lock.yaml",
    "distribution-digest-authority": "registry-integrity"
  },
  "commands": {
    "install": ["pnpm", "install", "--frozen-lockfile"],
    "build": ["pnpm", "run", "build"],
    "pack": ["pnpm", "pack", "--pack-destination", "<pack-output-directory>"]
  },
  "expected-subject": "pkg:npm/%40windlass-fixtures/scoped@1.0.0",
  "required-digests": ["sha512", "sha256"]
}
